[project]
name = "airtable-baserow-migrator"
version = "0.1.0"
description = "A tool to securely migrate data from AirTable to Baserow"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.33.0",
]
